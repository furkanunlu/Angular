import { ProductFiltrePipe } from './product-filtre.pipe';

describe('ProductFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
